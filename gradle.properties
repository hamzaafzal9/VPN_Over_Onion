# This enables the modern Android libraries used in your code
android.useAndroidX=true
android.enableJetifier=true

# This gives the builder enough memory so it doesn't crash
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
